/*
 * Stub header for Rockchip AIQ (RKAIQ) user API v2 - auto exposure
 *
 * Compilation stub only. Real header on board: /usr/include/rkaiq/uAPI2/rk_aiq_user_api2_ae.h
 */

#pragma once

#include <stdint.h>
#include <stdbool.h>

#ifdef __cplusplus
extern "C" {
#endif

/* ── Opaque context (forward decl) ─────────────────────────────────────────── */
struct rk_aiq_sys_ctx_s;
typedef struct rk_aiq_sys_ctx_s rk_aiq_sys_ctx_t;

/* ── AE operations (stubs) ─────────────────────────────────────────────────── */
/* Add AE-specific API declarations here as needed */

#ifdef __cplusplus
}
#endif
