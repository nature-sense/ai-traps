# ─── src/detection/pipeline/meson.build ─────────────────────────────────────────
# Pipeline sources for the detection trap.
# These are compiled as part of the ai-trap-detection executable.

pipeline_sources = files(
  'base_detection_pipeline.cpp',
)

pipeline_inc = include_directories('.')
